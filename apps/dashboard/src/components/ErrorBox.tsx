// Moved to ./Section, where it sits beside EmptyState -- the two are only
// meaningful in contrast with each other. Re-exported so existing imports
// keep working.
export { ErrorBox } from "./Section";
